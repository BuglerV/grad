{% extends 'form/form_container.twig' %}

{% block element %}
          <input type="{{ type }}" name="{{ name }}"{% if classes %} class="{{ classes }}"{% endif %}{% if attributes %} {{ attributes }}{% endif %}{% if value %} value="{{ value }}"{% endif %}>
{% endblock %}