{% extends 'form/form_container.twig' %}

{% block element %}
          <input type="file" name="{{ name }}{% if options.multiple %}[]" multiple{% else %}"{% endif %}{% if classes %} class="{{ classes }}"{% endif %}{% if attributes %} {{ attributes }}{% endif %}>
{% endblock %}